# Curation To-Do

Use this directory for generated or hand-written to-do lists that are not ready
to be passed directly to `scripts/stage_records.py`.

Move or copy individual records into `curation/staging/` when they are ready to
test.
